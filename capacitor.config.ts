import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.familychat',
  appName: 'family_chat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
